<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose paints everything; this only covers the window before the first frame. -->
    <style name="Theme.ShobdoJot" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/deep_indigo</item>
        <item name="android:statusBarColor">@color/deep_indigo</item>
        <item name="android:navigationBarColor">@color/deep_indigo</item>
    </style>
</resources>
